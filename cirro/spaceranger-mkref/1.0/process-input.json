{
  "custom": "$.inputs[0].dataPath|/**",
  "genome_dir": "$.dataset.params.genome_dir",
  "probes": "$.dataset.params.probes",
  "fasta": "$.dataset.params.genome_dir|/fasta/genome.fa",
  "genes": "$.dataset.params.genome_dir|/genes/genes.gtf",
  "output": "$.dataset.dataPath"
}
