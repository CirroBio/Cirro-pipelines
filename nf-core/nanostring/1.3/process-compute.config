profiles {
    standard {
        process {
            errorStrategy = 'retry'
            maxRetries = 2
        }
    }
}

// Nextflow plugins
plugins {
    id 'nf-schema@2.5.1' // Downgrade to match deployed Nextflow version
}