{
  "tools": "$.dataset.params.tool_selection.tools",
  "report": "$.dataset.params.tool_selection.reports",
  "genome": "$.dataset.params.ref_genome.genome",
  "trim": "$.dataset.params.trimming.bbmap",
  "max_memory": "$.dataset.params.compute_options.max_memory",
  "max_cpus": "$.dataset.params.compute_options.max_cpus",
  "outdir": "$.dataset.s3|/data/",
  "input": "$.dataset.s3|/config/manifest.csv",
  "genomes_base": "$.referencesBucketUri|/rnafusion/3.0.2/"
}
