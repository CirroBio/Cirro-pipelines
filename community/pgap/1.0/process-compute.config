// No docker.registry here: process-form.json offers 32 ncbi/pgap versions and only
// the default is mirrored, so redirecting would break the other 31.

process {
    errorStrategy = 'retry'
    maxRetries = 2
}
