process {
    errorStrategy = 'retry'
    maxRetries    = 2
    cpus          = { 8 * task.attempt }
    memory        = { 64.GB * task.attempt }
    container     = "quay.io/biocontainers/star:${params.star_version}"
    publishDir    = "${params.outdir}"
}
