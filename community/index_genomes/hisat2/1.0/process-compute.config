process {
    errorStrategy = 'retry'
    maxRetries    = 2
    cpus          = { 8 * task.attempt }
    memory        = { 32.GB * task.attempt }
    container     = "quay.io/biocontainers/hisat2:${params.hisat2_version}"
    publishDir    = "${params.outdir}"
}
